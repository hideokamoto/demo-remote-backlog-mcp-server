{
  "name": "remote-mcp-backlog-oauth",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests'"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.16.0",
    "backlog-js": "^0.16.0",
    "hono": "^4.12.25",
    "just-pick": "^4.2.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "6.0.3",
    "wrangler": "^4.100.0"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "0.28.1"
    }
  }
}